enum DiscountType {
  percentage,
  value,
}

enum AccessLevel {
  staff,
  administrator,
}

enum DataStatus {
  initial,
  loading,
  success,
  error,
}

enum FormStatus {
  initial,
  validating,
  invalid,
  valid,
  submitting,
  submitted,
  error,
  loading,
  success,
}

enum PaymentMethod {
  cash,
  credit,
  gcash,
}
