import 'package:fluent_ui/fluent_ui.dart';

class ArchivedProducts extends StatelessWidget {
  const ArchivedProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Archived Products");
  }
}
