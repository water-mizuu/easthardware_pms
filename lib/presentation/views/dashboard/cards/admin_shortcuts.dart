import 'package:fluent_ui/fluent_ui.dart';

class AdminShortcuts extends StatelessWidget {
  const AdminShortcuts({super.key});

  @override
  Widget build(BuildContext context) {
    /// TODO: Add if necessary.
    return Container();
  }
}
